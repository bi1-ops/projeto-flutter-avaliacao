import 'package:flutter/material.dart';

class AppPalette {
  static const Color primary = Color(0xFF0B6E8A);
  static const Color secondary = Color(0xFF2FB4C3);
  static const Color accent = Color(0xFFFFA552);
  static const Color background = Color(0xFFF4FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color text = Color(0xFF102A43);

  const AppPalette._();
}
